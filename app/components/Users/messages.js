import { defineMessages } from 'react-intl';

export const scope = 'components.Users';

export const usersMessages = defineMessages({
  hello: {
    id: `${scope}.hello`,
    defaultMessage: 'Usuarios'
  }
});
