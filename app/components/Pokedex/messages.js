import { defineMessages } from 'react-intl';

export const scope = 'components.Pokedex';

export const pokedexMessages = defineMessages({
  hello: {
    id: `${scope}.hello`,
    defaultMessage: 'Pokedex'
  }
});
